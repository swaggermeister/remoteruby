<h3>Log in</h3>
<h5 class="mb-4">No account? <%= link_to "Sign up!", new_registration_path(resource_name) %></h5>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <%= view.form_group(self, f, :email) do %>
    <%= f.email_field :email, class:"form-input", placeholder:"Email", autofocus: true, autocomplete: "email" %>
    <%= view.errors_hint_tag(f, :email) %>
  <% end %>
  <%= view.form_group(self, f, :password) do %>
    <%= f.password_field :password, class:"form-input", placeholder:"Password", autofocus: true, autocomplete: "current-password" %>
    <%= view.errors_hint_tag(f, :password) %>
  <% end %>
  <% if devise_mapping.rememberable? %>
    <div class="field mb-4">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>
  <div>
    <%= f.submit "Log in", class: "btn btn-block btn-primary my-2" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
