<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Remote Ruby on Rails Jobs</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
  </head>
  <body>
    <header class="navbar">
      <section class="navbar-section">
        <%= link_to root_path, class: "navbar-brand" do %>
          <%= image_tag asset_pack_path("media/images/logo.png"), alt: "Remote Ruby on Rails Jobs", width: 128 %>
        <% end %>
        <% if employer_signed_in? %>
          <%= link_to my_company_job_listings_path, class: "btn btn-link float-right" do %>
            My Account
            <%= image_tag view.employer_avatar_url(current_employer), class: "avatar" %>
          <% end %>
        <% else %>
          <%= link_to "Post a Job", new_employer_session_path, class: "btn btn-primary" %>
        <% end %>
      </section>
    </header>
    <div class="container mb-8">
      <div class="columns">
        <div class="col-lg-6 col-sm-8 col-xs-12 col-mx-auto col-4">
          <div class="container" id="app-layout-container">
            <%= render partial: "layouts/flash" %>
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
